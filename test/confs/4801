# exim test configuration 4801
exim_path = EXIM_PATH
keep_environment =

begin routers
  dnslookup:
     driver = dnslookup
     dnssec_require_domains = *
     transport = smtp

begin transports
  smtp:
     driver = smtp
