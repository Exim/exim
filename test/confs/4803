# exim test configuration 4803
exim_path = EXIM_PATH
keep_environment =
dns_trust_aa = *

begin routers
  dnslookup:
     driver = dnslookup
     dnssec_require_domains = *
     transport = smtp

begin transports
  smtp:
     driver = smtp
